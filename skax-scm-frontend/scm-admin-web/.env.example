# API 베이스 URL (브라우저에서 호출하는 호스트:포트)
#
# 권장: scm-api-gateway 경유 — docker compose 전체 기동 시 게이트웨이가 호스트 8081에 노출됩니다.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8081
#
# 대안: scm-common-service 직접 연결 — Gradle 등으로 common만 로컬 기동할 때(기본 server.port 8082).
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8082
