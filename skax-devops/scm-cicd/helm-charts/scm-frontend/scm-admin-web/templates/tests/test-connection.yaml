apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scm-admin-web.fullname" . }}-test-connection"
  labels:
    {{- include "scm-admin-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scm-admin-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
