{{- if .Values.backendTrafficPolicy.enabled -}}
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: BackendTrafficPolicy
metadata:
  name: {{ include "scm-admin-web.fullname" . }}-policy
  labels:
    {{- include "scm-admin-web.labels" . | nindent 4 }}
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: {{ include "scm-admin-web.fullname" . }}
  {{- with .Values.backendTrafficPolicy.timeout }}
  timeout:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
