apiVersion: v1
kind: Service
metadata:
  name: {{ include "scm-api-gateway.fullname" . }}
  labels:
    {{- include "scm-api-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scm-api-gateway.selectorLabels" . | nindent 4 }}
