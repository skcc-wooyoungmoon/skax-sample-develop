apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scm-common-service.fullname" . }}-test-connection"
  labels:
    {{- include "scm-common-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scm-common-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
